/node_modules
/.next
/out
/build
.DS_Store
*.pem
.vercel
next-env.d.ts

# Secrets. `.env*.local` alone missed a plain `.env`, which is the filename
# most tooling creates by default — this repo holds Razorpay, Shiprocket and
# Supabase service-role credentials, so that gap was worth closing.
.env
.env*
!.env.example
