import ShippingPolicyContent from '../../components/ShippingPolicyContent';

export const metadata = {
  title: 'Shipping Policy — Krta',
  description: 'How KRTA processes, ships, and delivers your hand-painted orders across India.',
};

export default function ShippingPage() {
  return <ShippingPolicyContent />;
}
