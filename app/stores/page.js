export const metadata = { title: 'Walk-in Stores — Krta' };

export default function StoresPage() {
  return (
    <div className="policy-page container">
      <h1 className="policy-title">Walk-in Stores</h1>
      <p className="policy-intro">
        We don't have any physical stores open just yet — but we're working on it. Check back soon.
      </p>
    </div>
  );
}
