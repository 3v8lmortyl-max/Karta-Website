import AboutContent from '../../components/AboutContent';

export const metadata = {
  title: 'Our Story — Krta',
  description: 'Krta is a luxury wearable-art brand — every piece individually hand-painted, founded by Prashanth Yadav.',
};

export default function AboutPage() {
  return <AboutContent />;
}
