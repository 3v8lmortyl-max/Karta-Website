import FaqContent from '../../components/FaqContent';

export const metadata = {
  title: "FAQ's — Krta",
  description: 'Answers to common questions about KRTA hand-painted products, orders, shipping, and customization.',
};

export default function FaqPage() {
  return <FaqContent />;
}
